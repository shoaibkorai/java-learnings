//import java.util.Scanner;

public class StringMethods {
    public static void main(String[] args) {
        //Scanner in = new Scanner(System.in);
       // String name = "   shoaib   ";
        String name = "shoaib";
        // name = in.nextLine();
        System.out.println(name);

        //     int len = name.length(); //to count the length of the string
        //    System.out.println("length of the stings is "+len);

        //    String ulen = name.toUpperCase(); // to make the string characters to uppercase
        //    System.out.println("length of the stings is "+ulen);

        //    String llen = name.toLowerCase(); // to make the string characters to lowercase
        //    System.out.println("length of the stings is "+llen);

        //    String tlen = name.trim(); // to terminate  the string spaces
        //    System.out.println("length of the stings is "+tlen);

        //     substring(int start ):        // to start the substring from your desired index
        //      System.out.println(name1.substring(1));

        //     substring(int start,int end ): returns a string from start to end , end index is included but
        //                                      end index is excluded
        //       System.out.println(name1.substring(1,5));

        //      name.replace("s","b") replaces char "S" to "b"
        //      System.out.println(name1.replace("s","b"));

        //       System.out.println(name1.startsWith("sho"));
        //       System.out.println(name1.startsWith("ho"));

        //       System.out.println(name1.endsWith("aib"));
        //       System.out.println(name1.endsWith("aid"));

        //       name.charAt(n); returns the char at n index n= that no of index
        //       System.out.println(name.charAt(2));

        //       name.indexOf("string")         it shows the index of the string on which it occurs
        //      System.out.println(name.indexOf("a"));

        //       name.indexOf("i",6); it finds the index no from your desired index
        //       System.out.println(name.indexOf("a",1));

        //       name.lastIndexOf('a');  it finds the index no from your desired index from last index
        //               System.out.println(name.lastIndexOf("a"));

        //               name.equals("shoaib"); it helps you to verify whethter the string is desired or not
        //               System.out.println(name.equals("shoaib")); // true
        //               System.out.println(name.equals("SHOAIB")); // false // bcz we know that strings are case sensitive so this will be false
        //               name.equalsIgnoreCase("SHOAIB"); // returns true if two strings are same by ignoring the case of charactor
        //               System.out.println(name.equalsIgnoreCase("SHOAIB"));







    }
}
